// DENTA CARE - Global Configuration // This is the single source of truth for all clinic information // Update these values once and they will reflect across the entire website  const clinicData = {   name: "aa Care",   doctor: "Mr. bhat",   phone: "+91 7357397888",   email: "info@dentacare.com",   location: ajmer , Maharashtra",   address: "miraz mall",   mapEmbed: "<iframe src='https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3780.4562!2d73.9189!3d18.5793!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x3bc2c7d5e5e5e5e5%3A0x5e5e5e5e5e5e5e5e!2sKharadi%2C%20Pune!5e0!3m2!1sen!2sin!4v1234567890' width='100%' height='450' style='border:0;' allowfullscreen='' loading='lazy'></iframe>",   hours: "1:00 AM – 7:00 PM" };  // Function to dynamically inject clinic data into HTML elements function injectClinicData() {   // Inject clinic name   document.querySelectorAll('[data-clinic="name"]').forEach(el => {     el.textContent = clinicData.name;   });    // Inject doctor name   document.querySelectorAll('[data-clinic="doctor"]').forEach(el => {     el.textContent = clinicData.doctor;   });    // Inject phone   document.querySelectorAll('[data-clinic="phone"]').forEach(el => {     if (el.tagName === 'A') el.href = `tel:${clinicData.phone}`;     el.textContent = clinicData.phone;   });    // Inject email   document.querySelectorAll('[data-clinic="email"]').forEach(el => {     if (el.tagName === 'A') el.href = `mailto:${clinicData.email}`;     el.textContent = clinicData.email;   });    // Inject location   document.querySelectorAll('[data-clinic="location"]').forEach(el => {     el.textContent = clinicData.location;   });    // Inject address   document.querySelectorAll('[data-clinic="address"]').forEach(el => {     el.textContent = clinicData.address;   });    // Inject hours   document.querySelectorAll('[data-clinic="hours"]').forEach(el => {     el.textContent = clinicData.hours;   });    // Inject map   document.querySelectorAll('[data-clinic="map"]').forEach(el => {     el.innerHTML = clinicData.mapEmbed;   }); }  // Run injection when DOM is ready document.addEventListener('DOMContentLoaded', injectClinicData);